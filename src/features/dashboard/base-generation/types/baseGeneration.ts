export type BaseGenerationSummaryMetric = {
  label: string;
  values: string[];
};

export type BaseGenerationChartDatum = {
  value: number;
  name: string;
};

export type BaseGenerationTableRow = string[];
